<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.server.dao.UserInfoDao">
	<!-- sql元素可以用来定义可重用的SQL代码片段 -->
	<sql id="table_column">
		id,user_name,password,email,mobile,status,deleted,create_time,update_time
	</sql>
	<sql id="table_name">
		user_info
	</sql>

	<select id="getAll" resultType="com.demo.server.bean.entity.UserInfo">
		select
		<include refid="table_column" />
		from
		<include refid="table_name" />
	</select>

	<select id="getById" resultType="UserInfo">
		select
		<include refid="table_column" />
		from
		<include refid="table_name" />
		where id = #{id}
	</select>

	<delete id="deleteById">
		delete from
		<include refid="table_name" />
		where id = #{id}
	</delete>

</mapper>
